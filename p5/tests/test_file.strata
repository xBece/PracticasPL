main {
	var
		int a, b, c, j, m, h;
		list int li;
		float f;
	endvar

	proc procedimientoA (int a1, mut int a2, int a3) {
		if (a3 > 10)
			a2 = a3;
		else
			a2= a1 ;
	}

	a = 4;
	b = 3;
	c = 1;
	j = 8;

	procedimientoA(a, b, c+1);

	if (a > b) {
		a = b + c + 8;
		m = j*4 - a;
	} else {
		h = 1;
	}

	print a,2+4,c,b;

	li = [1,1+2,3];
	li[2] = c;

	c = li[0];
	print c;

	remove(li, 0);

	print li[0], #li;

	insert(li, #li, 10);
	print #li, li[#li-1];

	read a, f;
	print a;
	print "el resultado es ", f*2.0, "mateos es muy guapo";

}